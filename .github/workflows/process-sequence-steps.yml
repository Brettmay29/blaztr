name: Process Sequence Steps

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  process-steps:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger processSequenceSteps
        run: |
          curl -X POST "https://blaztr.base44.app/api/apps/69b02505f9b4d10dd348ac34/functions/processSequenceSteps" \
            -H "Content-Type: application/json" \
            -H "api_key: ${{ secrets.BLAZTR_API_KEY }}" \
            -d '{}'

      - name: Trigger checkReplies
        run: |
          curl -X POST "https://blaztr.base44.app/api/apps/69b02505f9b4d10dd348ac34/functions/checkReplies" \
            -H "Content-Type: application/json" \
            -H "api_key: ${{ secrets.BLAZTR_API_KEY }}" \
            -d '{}'
